# Server

Flask REST API for PatternLab Data Analyzer.

The server handles authentication, CSV upload, dataset profiling, cleaning, metadata, exploratory analysis, feature ranking, model comparison, report generation, history, and SQLite persistence.

Use the project root [README.md](../README.md) for installation, startup, and test commands.
